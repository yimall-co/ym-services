// import { z } from 'zod';
// import { initChatModel, tool } from 'langchain';
// import { TavilySearch } from '@langchain/tavily';

// type SearchToolInput = {
//     query: string;
//     maxResults: number;
//     topic: string;
//     includeRawContent: boolean;
// };

// const handler = (input: SearchToolInput) => {
//     const { query, maxResults, topic, includeRawContent } = input;

//     const tavilySearch = new TavilySearch({});

// };

// export const searchTool = tool(handler, {
//     name: 'web_search',
//     description: '',
// });
